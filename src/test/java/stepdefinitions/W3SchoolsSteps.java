package stepdefinitions;

import cucumber.api.java8.En;

public class W3SchoolsSteps implements En {
    public W3SchoolsSteps() {
        Then("^I should see \"([^\"]*)\" as country for \"([^\"]*)\" company$", (String arg0, String arg1) -> {
        });
    }
}
